package collections;

/**
 * actually it's a linked list, but it's ordered, so if we want to find some element,
 * we want to use ordered feature, so we record more pointer
 */
public class SkipList {

}
